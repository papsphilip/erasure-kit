---
phase: 06-frontend-sending-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/relay-client.js
  - src/lib/relay-client.test.js
  - src/lib/status-tracker.js
  - src/lib/status-tracker.test.js
autonomous: true
requirements: [RELAY-05, STAT-01, STAT-04, STAT-05]
must_haves:
  truths:
    - "relay-client module can POST encrypted payloads to the Worker and parse success/error responses"
    - "relay-client module can DELETE temp addresses via the Worker"
    - "STATUS.FAILED exists in the status lifecycle with red color tokens"
    - "markBrokerFailed() records the error code and message in broker status"
    - "getStatusCounts() includes a 'failed' count"
    - "getProgressPercent() excludes failed brokers from resolution count"
  artifacts:
    - path: "src/lib/relay-client.js"
      provides: "fetch() wrapper for Worker API (POST /send, DELETE /address)"
      exports: ["relaySend", "relayDeleteAddress", "RELAY_URL"]
    - path: "src/lib/relay-client.test.js"
      provides: "Unit tests for relay-client module"
      min_lines: 60
    - path: "src/lib/status-tracker.js"
      provides: "Updated status tracker with FAILED status"
      exports: ["STATUS", "markBrokerFailed", "getStatusCounts"]
    - path: "src/lib/status-tracker.test.js"
      provides: "Unit tests for FAILED status, counts, and progress"
      min_lines: 80
  key_links:
    - from: "src/lib/relay-client.js"
      to: "https://api.erasurekit.uk/send"
      via: "fetch POST with JSON payload"
      pattern: "fetch.*RELAY_URL.*send"
    - from: "src/lib/relay-client.js"
      to: "https://api.erasurekit.uk/address"
      via: "fetch DELETE with slug query param"
      pattern: "fetch.*RELAY_URL.*address"
    - from: "src/lib/status-tracker.js"
      to: "campaign signal"
      via: "immutable campaign.value update"
      pattern: "campaign\\.value = \\{"
---

<objective>
Create the relay-client module and extend the status tracker with FAILED status support.

Purpose: These are the foundational building blocks that Plan 02 (email-sender refactor) and Plan 03 (UI components) depend on. relay-client wraps the Worker API. FAILED status enables error handling and retry flows.

Output: Two new/modified modules with comprehensive test coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-frontend-sending-integration/06-CONTEXT.md
@.planning/phases/06-frontend-sending-integration/06-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From worker/src/routes/send.js (Worker POST /send contract):
- Payload: { to, subject, encryptedBody, wrappedKey, iv, tempAddress, publicKey }
- Success: { success: true, data: { emailId, tempAddress } }
- Error 400: { success: false, error: { code: 'INVALID_PAYLOAD', message: '...' } }
- Error 429: { success: false, error: { code: 'RATE_LIMITED', message: '...' } } + Retry-After header
- Error 502: { success: false, error: { code: 'RESEND_FAILED', message: '...' } }

From worker/src/routes/address.js (Worker DELETE /address contract):
- Query param: ?slug=a7k9x3mq (8 lowercase alphanumeric chars)
- Success: { success: true, data: { deleted: 'a7k9x3mq' } }
- Error 400: { success: false, error: { code: 'INVALID_PAYLOAD', message: '...' } }

From src/lib/status-tracker.js (current STATUS enum):
```javascript
export const STATUS = {
  NOT_SELECTED: 'not_selected',
  SELECTED: 'selected',
  AWAITING: 'awaiting',
  CONFIRMED: 'confirmed',
  REJECTED: 'rejected',
  ESCALATED: 'escalated',
  OVERDUE: 'overdue',
};
```

From src/lib/campaign.js (campaign signal pattern):
```javascript
export const campaign = signal(_stored || createEmptyCampaign());
// Immutable update pattern:
campaign.value = { ...campaign.value, updatedAt: new Date().toISOString(), ... };
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create relay-client module with tests</name>
  <files>src/lib/relay-client.js, src/lib/relay-client.test.js</files>
  <read_first>
    - worker/src/routes/send.js (Worker POST /send contract -- payload format, response envelope, error codes)
    - worker/src/routes/address.js (Worker DELETE /address contract -- slug query param, response format)
    - worker/src/lib/validate.js (payload validation rules -- what fields are required)
    - src/lib/crypto.test.js (existing test patterns for this project)
    - vitest.config.js (test configuration -- environment: jsdom, include pattern)
  </read_first>
  <behavior>
    - Test 1: relaySend() with successful response returns { success: true, data: { emailId, tempAddress } }
    - Test 2: relaySend() with 400 response returns { success: false, error: { code: 'INVALID_PAYLOAD', message } }
    - Test 3: relaySend() with 429 response returns { success: false, error: { code: 'RATE_LIMITED', message }, retryAfter: number }
    - Test 4: relaySend() with 429 response and missing Retry-After header defaults retryAfter to 60
    - Test 5: relaySend() with 502 response returns { success: false, error: { code: 'RESEND_FAILED', message } }
    - Test 6: relaySend() sends correct Content-Type header and JSON body
    - Test 7: relayDeleteAddress() with successful response returns { success: true, data: { deleted: slug } }
    - Test 8: relayDeleteAddress() with 400 response returns error object
    - Test 9: relayDeleteAddress() constructs URL with slug query parameter
    - Test 10: relaySend() with network error (fetch throws) propagates the error
  </behavior>
  <action>
    Create src/lib/relay-client.js with:

    Configuration constants at module top (per dynamic-not-hardcoded convention):
    ```
    const RELAY_URL = 'https://api.erasurekit.uk';
    ```
    Export RELAY_URL for use by other modules.

    relaySend(payload) function:
    - Accepts object: { to, subject, encryptedBody, wrappedKey, iv, tempAddress, publicKey }
    - POST to `${RELAY_URL}/send` with Content-Type: application/json header
    - Body: JSON.stringify(payload)
    - Parse response as JSON
    - If response.status === 429: extract Retry-After header, parse with `parseInt(response.headers.get('Retry-After')) || 60`, return { ...result, retryAfter: parsedValue }
    - Return the parsed JSON result for all other status codes
    - Do NOT catch fetch errors -- let them propagate to the caller for error handling

    relayDeleteAddress(slug) function:
    - Accepts string slug (8-char alphanumeric)
    - DELETE to `${RELAY_URL}/address?slug=${slug}`
    - Parse response as JSON and return it
    - Do NOT catch fetch errors

    Create src/lib/relay-client.test.js:
    - Use vitest (describe, it, expect, vi)
    - Mock global.fetch using vi.fn() in beforeEach, restore in afterEach
    - Test all behaviors listed above
    - For 429 tests, mock Response with headers Map containing 'Retry-After'
    - For network error test, make fetch mock reject with new Error('Network error')
  </action>
  <verify>
    <automated>cd F:/_Github/Virgo/tools/erasure-kit && npx vitest run src/lib/relay-client.test.js --reporter=verbose</automated>
  </verify>
  <done>
    - src/lib/relay-client.js exists and exports relaySend, relayDeleteAddress, RELAY_URL
    - src/lib/relay-client.test.js exists with 10+ test cases
    - All relay-client tests pass
    - relaySend posts to /send with correct headers and parses all response types (200, 400, 429, 502)
    - relayDeleteAddress sends DELETE to /address?slug=... and parses response
    - 429 responses include retryAfter parsed from Retry-After header with fallback to 60
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add STATUS.FAILED to status-tracker with tests</name>
  <files>src/lib/status-tracker.js, src/lib/status-tracker.test.js</files>
  <read_first>
    - src/lib/status-tracker.js (current STATUS enum, STATUS_LABELS, STATUS_COLORS, getStatusCounts, getProgressPercent, markBrokerSent, updateBrokerStatus patterns)
    - src/lib/campaign.js (campaign signal, createEmptyCampaign -- understand the immutable update pattern)
    - src/lib/campaign.test.js (existing test patterns, signal mocking approach)
  </read_first>
  <behavior>
    - Test 1: STATUS.FAILED equals 'failed'
    - Test 2: STATUS_LABELS[STATUS.FAILED] equals 'Failed'
    - Test 3: STATUS_COLORS[STATUS.FAILED] has bg, text, and dot properties with red color values
    - Test 4: markBrokerFailed(brokerId, errorCode, errorMessage) sets status to FAILED with error details
    - Test 5: markBrokerFailed() preserves existing sentAt and deadline values
    - Test 6: markBrokerFailed() adds history entry with error details
    - Test 7: getStatusCounts() includes 'failed' key in returned object
    - Test 8: getStatusCounts() counts failed brokers correctly (failed brokers should NOT be counted in 'sent')
    - Test 9: getProgressPercent() does NOT count failed brokers as resolved (failed = still pending)
    - Test 10: retryBroker(brokerId) resets status from FAILED back to SELECTED for re-send
  </behavior>
  <action>
    Modify src/lib/status-tracker.js:

    1. Add to STATUS enum:
       FAILED: 'failed'

    2. Add to STATUS_LABELS:
       [STATUS.FAILED]: 'Failed'

    3. Add to STATUS_COLORS (per D-04: failed shows red):
       [STATUS.FAILED]: { bg: 'bg-red-500/15', text: 'text-red-500', dot: 'bg-red-500' }

    4. Add markBrokerFailed(brokerId, errorCode, errorMessage) function:
       - Get existing status entry or create default { status: STATUS.SELECTED, sentAt: null, deadline: null, history: [] }
       - Create history entry: { status: STATUS.FAILED, at: new Date().toISOString(), details: `${errorCode}: ${errorMessage}` }
       - Update campaign.value immutably: set status to STATUS.FAILED, add error object { code: errorCode, message: errorMessage }, preserve sentAt and deadline, append history entry
       - Pattern: same immutable spread as markBrokerSent()

    5. Add retryBroker(brokerId) function:
       - Get existing status entry
       - Create history entry: { status: STATUS.SELECTED, at: new Date().toISOString(), details: 'Queued for retry' }
       - Update campaign.value: set status back to STATUS.SELECTED, clear error field, preserve sentAt and deadline, append history entry

    6. Update getStatusCounts():
       - Add `failed: 0` to initial counts object
       - Add case for STATUS.FAILED in switch: increment `counts.failed` (do NOT increment counts.sent -- failed brokers were not successfully sent)

    7. getProgressPercent() -- NO CHANGE NEEDED. It already computes (confirmed + rejected) / total. Since failed is not confirmed or rejected, failed brokers are automatically excluded.

    Create src/lib/status-tracker.test.js:
    - Import from status-tracker.js: STATUS, STATUS_LABELS, STATUS_COLORS, markBrokerFailed, retryBroker, getStatusCounts, getProgressPercent, markBrokerSent, getBrokerStatus
    - Import campaign signal from campaign.js
    - In beforeEach: reset campaign.value to createEmptyCampaign() with brokers.selected = ['broker-1', 'broker-2', 'broker-3'] and statuses = {}
    - Test all behaviors listed above
    - For getStatusCounts tests: set up specific statuses (one AWAITING, one FAILED, one CONFIRMED) and verify counts
    - For getProgressPercent: with 3 brokers (1 confirmed, 1 failed, 1 awaiting), progress should be 33% (1/3), not 67% (2/3)
  </action>
  <verify>
    <automated>cd F:/_Github/Virgo/tools/erasure-kit && npx vitest run src/lib/status-tracker.test.js --reporter=verbose</automated>
  </verify>
  <done>
    - STATUS.FAILED = 'failed' exists in status-tracker.js
    - STATUS_LABELS contains 'Failed' label
    - STATUS_COLORS contains red color tokens for FAILED
    - markBrokerFailed() is exported and sets FAILED status with error { code, message }
    - retryBroker() is exported and resets FAILED status back to SELECTED
    - getStatusCounts() returns object with 'failed' key, failed brokers not in 'sent' count
    - getProgressPercent() excludes failed from numerator
    - All status-tracker tests pass
  </done>
</task>

</tasks>

<verification>
Run the full test suite to ensure no regressions:
```bash
cd F:/_Github/Virgo/tools/erasure-kit && npx vitest run --reporter=verbose
```
All existing tests (crypto, campaign, notifications, template-engine) plus new tests (relay-client, status-tracker) must pass.
</verification>

<success_criteria>
1. relay-client.js exists with relaySend() and relayDeleteAddress() exports
2. status-tracker.js has STATUS.FAILED, markBrokerFailed(), retryBroker(), updated getStatusCounts()
3. Both modules have passing test suites
4. No regressions in existing test suite
</success_criteria>

<output>
After completion, create `.planning/phases/06-frontend-sending-integration/06-01-SUMMARY.md`
</output>
