---
phase: 01-app-shell-and-distribution
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/app.js
  - src/components/Header.js
  - src/components/Stepper.js
  - src/components/ThemeToggle.js
  - src/components/HamburgerMenu.js
  - src/components/WelcomeScreen.js
  - src/components/Footer.js
  - src/pages/Identity.js
  - src/pages/Brokers.js
  - src/pages/Send.js
  - src/pages/Track.js
  - src/pages/About.js
autonomous: true
requirements:
  - DIST-01
  - DIST-02
  - DIST-03

must_haves:
  truths:
    - "User opens the app and sees a welcome screen with 'Take back your data' heading and 'Get Started' CTA button"
    - "User can click any of the 4 stepper steps (Identity, Brokers, Send, Track) to navigate freely between placeholder pages"
    - "User can toggle between dark and light mode via the header icon, and the preference persists across page reloads"
    - "User can open the hamburger menu to access About page (Legal Reference and Escalation show placeholder labels)"
    - "User can click footer links to navigate to Legal Reference and About"
    - "Running pnpm build produces dist/Erasure-Kit/ containing erasure-kit.html, brokers.json, and README.md -- and erasure-kit.html works when opened in a browser"
  artifacts:
    - path: "src/app.js"
      provides: "Root App component wiring header, stepper, pages, footer"
      min_lines: 30
    - path: "src/components/Header.js"
      provides: "Sticky header with brand text, theme toggle, hamburger menu"
      contains: "ErasureKit"
    - path: "src/components/Stepper.js"
      provides: "4-step wizard navigation with visual progress indicators"
      contains: "Identity"
    - path: "src/components/WelcomeScreen.js"
      provides: "Landing page with hero heading, CTA, privacy statement"
      contains: "Take back your data"
    - path: "src/components/HamburgerMenu.js"
      provides: "Slide-in secondary navigation panel"
      contains: "About"
    - path: "src/components/ThemeToggle.js"
      provides: "Dark/light mode toggle button with sun/moon SVG icons"
      contains: "toggleTheme"
    - path: "src/components/Footer.js"
      provides: "Footer with navigation links"
      contains: "Legal Reference"
  key_links:
    - from: "src/app.js"
      to: "src/lib/router.js"
      via: "import currentPage signal"
      pattern: "import.*currentPage.*from.*router"
    - from: "src/components/Stepper.js"
      to: "src/lib/router.js"
      via: "navigateTo on step click"
      pattern: "navigateTo"
    - from: "src/components/ThemeToggle.js"
      to: "src/lib/theme.js"
      via: "toggleTheme on click"
      pattern: "toggleTheme"
    - from: "src/components/WelcomeScreen.js"
      to: "src/lib/router.js"
      via: "navigateTo('identity') on CTA click"
      pattern: "navigateTo.*identity"
---

<objective>
Build the complete interactive app shell: root App component with signal-based routing, header with brand/theme/menu, 4-step wizard stepper with free jumping, welcome screen with hero content and CTA, hamburger menu with secondary navigation, placeholder step pages, About page, and footer. Then verify the production build produces a working dist/Erasure-Kit/ folder.

Purpose: This is the user-facing deliverable of Phase 1 -- a complete, navigable app shell that demonstrates the two-mode architecture works end-to-end.
Output: A fully interactive app shell that works in both dev mode (CDN) and production build (single HTML file).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-app-shell-and-distribution/01-CONTEXT.md
@.planning/phases/01-app-shell-and-distribution/01-RESEARCH.md
@.planning/phases/01-app-shell-and-distribution/01-UI-SPEC.md
@.planning/phases/01-app-shell-and-distribution/01-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts from Plan 01 that this plan depends on. -->

From src/lib/router.js:
```javascript
export const currentPage = signal('welcome');
export const completedSteps = signal(new Set());
export function navigateTo(page) { currentPage.value = page; }
export function markStepComplete(stepId) { ... }
```

From src/lib/theme.js:
```javascript
export const isDark = signal(true); // default dark
export function toggleTheme() { isDark.value = !isDark.value; }
```

From src/lib/brokers-loader.js:
```javascript
export async function loadBrokers() { ... } // returns Promise<Array>
```

CSS Custom Properties (from index.html):
- `--ek-primary: #0EA5E9`  `--ek-accent: #06B6D4`
- `--ek-surface: #0F172A` (dark) / `#F8FAFC` (light)
- `--ek-surface-alt: #1E293B` (dark) / `#F1F5F9` (light)
- `--ek-text: #F8FAFC` (dark) / `#0F172A` (light)
- `--ek-text-muted: #94A3B8` (dark) / `#64748B` (light)
- `--ek-border: #334155` (dark) / `#E2E8F0` (light)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: App shell components -- Header, Stepper, ThemeToggle, HamburgerMenu, Footer</name>
  <files>src/components/Header.js, src/components/Stepper.js, src/components/ThemeToggle.js, src/components/HamburgerMenu.js, src/components/Footer.js</files>
  <read_first>
    src/index.html
    src/lib/router.js
    src/lib/theme.js
    .planning/phases/01-app-shell-and-distribution/01-UI-SPEC.md
    .planning/phases/01-app-shell-and-distribution/01-RESEARCH.md
  </read_first>
  <action>
    Create all shell components using Preact + HTM tagged templates. All components use `import { html } from 'htm/preact';` and CSS custom properties from index.html.

    **1. src/components/ThemeToggle.js** (per D-06, UI-SPEC ThemeToggle):
    - Import `html` from `htm/preact`, `isDark` and `toggleTheme` from `../lib/theme.js`
    - Render a `<button>` with `onClick=${toggleTheme}`
    - Size: 40px (`w-10 h-10`), `rounded-lg` (per D-11)
    - `aria-label` dynamically set: `isDark.value ? 'Switch to light mode' : 'Switch to dark mode'`
    - Show moon SVG when dark, sun SVG when light (20px icons):
      - Sun SVG: `<svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 3v1m0 16v1m9-9h-1M4 12H3m15.364 6.364l-.707-.707M6.343 6.343l-.707-.707m12.728 0l-.707.707M6.343 17.657l-.707.707M16 12a4 4 0 11-8 0 4 4 0 018 0z"/></svg>`
      - Moon SVG: `<svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M20.354 15.354A9 9 0 018.646 3.646 9.003 9.003 0 0012 21a9.003 9.003 0 008.354-5.646z"/></svg>`
    - Styling: transparent bg, hover `bg-[var(--ek-surface-alt)]`, text `text-[var(--ek-text-muted)]`, hover text `hover:text-[var(--ek-primary)]`
    - Focus: `focus-visible:ring-2 focus-visible:ring-sky-500 focus-visible:ring-offset-2 focus-visible:ring-offset-[var(--ek-surface)]`

    **2. src/components/HamburgerMenu.js** (per D-04, UI-SPEC HamburgerMenu):
    - Import `html` from `htm/preact`, `{ signal }` from `@preact/signals`, `navigateTo` from `../lib/router.js`
    - Internal `isOpen` signal: `const isOpen = signal(false)`
    - Trigger button: 40px (`w-10 h-10`), `rounded-lg`, hamburger icon SVG (three horizontal lines):
      `<svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"/></svg>`
    - `aria-expanded=${isOpen.value}`, `aria-label="Menu"`
    - When open: backdrop overlay `fixed inset-0 bg-black/50 z-40` + slide-in panel from right
    - Panel: `fixed top-0 right-0 h-full w-[280px] bg-[var(--ek-surface-alt)] z-50 rounded-l-xl shadow-xl`
    - Panel header: close button (X icon) top-right, 40px
    - Menu items (each 48px tall `h-12`, full-width, `rounded-lg` hover):
      - "Legal Reference" -- navigates to `'legal'` (placeholder page in future phase, for now navigateTo('legal'))
      - "Escalation" -- navigates to `'escalation'` (placeholder page in future phase)
      - "About" -- navigates to `'about'`
    - Each item click: `navigateTo(page)` then `isOpen.value = false`
    - Transition: panel slides in with CSS `transition-transform duration-200 ease-out`, use `translate-x-full` when closed, `translate-x-0` when open
    - Backdrop click closes menu
    - Keyboard: Escape key closes menu (add `onKeyDown` handler on the panel)

    **3. src/components/Stepper.js** (per D-01, D-02, D-03, D-05, UI-SPEC Stepper/StepCircle/StepConnector):
    - Import `html` from `htm/preact`, `currentPage`, `completedSteps`, `navigateTo` from `../lib/router.js`
    - Define STEPS array:
      ```javascript
      const STEPS = [
        { id: 'identity', label: 'Identity', number: 1 },
        { id: 'brokers',  label: 'Brokers',  number: 2 },
        { id: 'send',     label: 'Send',     number: 3 },
        { id: 'track',    label: 'Track',    number: 4 },
      ];
      ```
    - Render `<nav class="flex items-center justify-center gap-2 py-3 px-4 bg-[var(--ek-surface-alt)] border-b border-[var(--ek-border)]" aria-label="Progress">`
    - For each step:
      - Determine `isCurrent = currentPage.value === step.id`
      - Determine `isCompleted = completedSteps.value.has(step.id)`
      - Between steps (i > 0): connector line `<div class="w-8 h-0.5 ${isCompleted || isPreviousCompleted ? 'bg-[var(--ek-primary)]' : 'bg-[var(--ek-border)]'}"></div>`
        - Connector is accent-colored if the step BEFORE it is completed
      - Step button: `<button onClick=${() => navigateTo(step.id)}>`
        - All steps always clickable (D-02 -- free jumping, no locks)
        - `aria-current=${isCurrent ? 'step' : undefined}`
        - Focus: `focus-visible:ring-2 focus-visible:ring-sky-500 focus-visible:ring-offset-2`
      - Circle (28px = `w-7 h-7 rounded-full` per UI-SPEC):
        - Current step: `bg-[var(--ek-primary)] text-white border-2 border-[var(--ek-primary)]`
        - Completed step: `bg-[var(--ek-primary)] text-white border-2 border-[var(--ek-primary)]` with checkmark SVG instead of number:
          `<svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="3" d="M5 13l4 4L19 7"/></svg>`
        - Future step: `text-[var(--ek-text-muted)] border-2 border-[var(--ek-border)]`
      - Label: `<span class="hidden sm:inline text-sm font-medium">${step.label}</span>` (hidden on mobile per D-05)
        - Current: `text-[var(--ek-primary)]`
        - Completed: `text-[var(--ek-primary)]`
        - Future: `text-[var(--ek-text-muted)]`

    **4. src/components/Header.js** (per UI-SPEC Header):
    - Import `html` from `htm/preact`, `ThemeToggle` from `./ThemeToggle.js`, `HamburgerMenu` from `./HamburgerMenu.js`
    - Render: `<header class="sticky top-0 z-30 h-14 flex items-center justify-between px-4 bg-[var(--ek-surface-alt)] border-b border-[var(--ek-border)]">`
    - Left side: `<span class="text-xl font-semibold text-[var(--ek-primary)]">ErasureKit</span>` (per D-10, brand text only)
    - Right side: flex row with `<${ThemeToggle} />` and `<${HamburgerMenu} />` with `gap-1`

    **5. src/components/Footer.js** (per UI-SPEC Footer):
    - Import `html` from `htm/preact`, `navigateTo` from `../lib/router.js`
    - Render: `<footer class="h-12 flex items-center justify-center gap-4 px-4 border-t border-[var(--ek-border)] text-sm text-[var(--ek-text-muted)]">`
    - Two links (styled as buttons): "Legal Reference" and "About"
    - Each: `<button onClick=${() => navigateTo('legal')} class="hover:text-[var(--ek-primary)] transition-colors focus-visible:ring-2 focus-visible:ring-sky-500 rounded">Legal Reference</button>`
    - Same pattern for About linking to `navigateTo('about')`
  </action>
  <verify>
    <automated>cd "F:/_Github/Virgo/tools/erasure-kit" && test -f src/components/Header.js && test -f src/components/Stepper.js && test -f src/components/ThemeToggle.js && test -f src/components/HamburgerMenu.js && test -f src/components/Footer.js && grep -l "ErasureKit" src/components/Header.js && grep -l "toggleTheme" src/components/ThemeToggle.js && grep -l "navigateTo" src/components/Stepper.js && echo "ALL SHELL COMPONENTS EXIST"</automated>
  </verify>
  <acceptance_criteria>
    - src/components/ThemeToggle.js imports `isDark` and `toggleTheme` from `../lib/theme.js`
    - src/components/ThemeToggle.js contains sun SVG and moon SVG (conditional on isDark.value)
    - src/components/ThemeToggle.js has `aria-label` that changes between "Switch to light mode" and "Switch to dark mode"
    - src/components/ThemeToggle.js button has `w-10 h-10 rounded-lg`
    - src/components/HamburgerMenu.js contains `isOpen` signal
    - src/components/HamburgerMenu.js contains three menu items: "Legal Reference", "Escalation", "About"
    - src/components/HamburgerMenu.js has backdrop `bg-black/50` and panel `w-[280px]`
    - src/components/HamburgerMenu.js calls `navigateTo` on menu item click and sets `isOpen.value = false`
    - src/components/HamburgerMenu.js has `aria-expanded` on trigger button
    - src/components/Stepper.js defines STEPS array with 4 entries: identity, brokers, send, track
    - src/components/Stepper.js has `<nav` with `aria-label="Progress"`
    - src/components/Stepper.js renders circles with `w-7 h-7 rounded-full`
    - src/components/Stepper.js calls `navigateTo(step.id)` on click (all steps always clickable per D-02)
    - src/components/Stepper.js labels hidden on mobile: `hidden sm:inline`
    - src/components/Stepper.js has checkmark SVG for completed steps
    - src/components/Header.js contains `ErasureKit` text with `text-[var(--ek-primary)]`
    - src/components/Header.js renders ThemeToggle and HamburgerMenu components
    - src/components/Header.js has `sticky top-0` and `h-14`
    - src/components/Footer.js contains "Legal Reference" and "About" as clickable elements
    - src/components/Footer.js has `border-t border-[var(--ek-border)]`
  </acceptance_criteria>
  <done>All 5 shell components (Header, Stepper, ThemeToggle, HamburgerMenu, Footer) exist with correct structure, accessibility attributes, responsive behavior, and proper imports from lib modules</done>
</task>

<task type="auto">
  <name>Task 2: Welcome screen, placeholder pages, About page, and root App component with build verification</name>
  <files>src/components/WelcomeScreen.js, src/pages/Identity.js, src/pages/Brokers.js, src/pages/Send.js, src/pages/Track.js, src/pages/About.js, src/app.js</files>
  <read_first>
    src/lib/router.js
    src/lib/theme.js
    src/lib/brokers-loader.js
    src/components/Header.js
    src/components/Stepper.js
    src/components/Footer.js
    .planning/phases/01-app-shell-and-distribution/01-UI-SPEC.md
    .planning/phases/01-app-shell-and-distribution/01-CONTEXT.md
  </read_first>
  <action>
    Create the page components and the root App that wires everything together.

    **1. src/components/WelcomeScreen.js** (per D-12, D-13, D-14, UI-SPEC WelcomeScreen, Copywriting Contract):
    - Import `html` from `htm/preact`, `navigateTo` from `../lib/router.js`
    - Check localStorage for returning user: `const hasExistingCampaign = typeof localStorage !== 'undefined' && localStorage.getItem('ek-campaign') !== null;` (per D-13)
    - Render centered content with `max-w-2xl mx-auto px-4 sm:px-6 py-12 sm:py-16`
    - Shield emoji at top: `<div class="text-5xl mb-6 text-center">🛡️</div>`
    - Display heading (focal point per UI-SPEC): `<h1 class="text-2xl sm:text-3xl font-semibold text-center leading-none mb-4">Take back your data</h1>`
    - Body paragraph: `<p class="text-base text-[var(--ek-text-muted)] text-center mb-8 leading-normal">ErasureKit sends legally-compliant GDPR erasure requests to data brokers on your behalf. Free, private, no accounts required.</p>`
    - 4 bullet points in a vertical list with `gap-3`, each with an inline SVG icon (16px, accent colored) and label text:
      1. User icon + "Enter your identity details"
      2. Database icon + "Select brokers holding your data"
      3. Send icon + "Send erasure requests in one click"
      4. Chart icon + "Track responses and enforce deadlines"
    - Bullet SVG icons (simple, 16px inline):
      - User: `<svg class="w-4 h-4 text-[var(--ek-primary)] flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"/></svg>`
      - Database: `<svg class="w-4 h-4 text-[var(--ek-primary)] flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 7v10c0 2.21 3.582 4 8 4s8-1.79 8-4V7M4 7c0 2.21 3.582 4 8 4s8-1.79 8-4M4 7c0-2.21 3.582-4 8-4s8 1.79 8 4"/></svg>`
      - Send: `<svg class="w-4 h-4 text-[var(--ek-primary)] flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"/></svg>`
      - Chart: `<svg class="w-4 h-4 text-[var(--ek-primary)] flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z"/></svg>`
    - CTA button (focal point per UI-SPEC PrimaryCTA):
      - If returning user: "Resume Campaign", onClick navigates to last step (or 'identity' as default)
      - If new user: "Get Started", onClick navigates to 'identity'
      - Styling: `w-full sm:w-auto h-12 px-8 bg-[var(--ek-primary)] text-white text-base font-semibold rounded-lg hover:brightness-90 active:brightness-85 transition-all duration-150 hover:scale-[1.02] focus-visible:ring-2 focus-visible:ring-sky-500 focus-visible:ring-offset-2 focus-visible:ring-offset-[var(--ek-surface)]`
    - Privacy statement at bottom (per D-14, UI-SPEC PrivacyStatement):
      - `<p class="text-sm text-[var(--ek-text-muted)] text-center mt-8 flex items-center justify-center gap-1.5">`
      - Lock icon (14px inline SVG): `<svg class="w-3.5 h-3.5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 15v2m-6 4h12a2 2 0 002-2v-6a2 2 0 00-2-2H6a2 2 0 00-2 2v6a2 2 0 002 2zm10-10V7a4 4 0 00-8 0v4h8z"/></svg>`
      - Text: "No data leaves your browser. No accounts. No tracking."

    **2. src/pages/Identity.js** (placeholder per UI-SPEC PlaceholderPage):
    - Import `html` from `htm/preact`
    - Render centered content: heading "Identity" + body "Coming in Phase 2"
    - Construction icon SVG (20px): wrench or hammer icon
    - Styling: `max-w-2xl mx-auto px-4 py-16 text-center`

    **3. src/pages/Brokers.js** (placeholder):
    - Same pattern as Identity: heading "Brokers" + body "Coming in Phase 3"

    **4. src/pages/Send.js** (placeholder):
    - Same pattern: heading "Send" + body "Coming in Phase 5"

    **5. src/pages/Track.js** (placeholder):
    - Same pattern: heading "Track" + body "Coming in Phase 5"

    **6. src/pages/About.js** (per D-15, UI-SPEC AboutPage, Copywriting Contract):
    - Import `html` from `htm/preact`
    - Render: `<div class="max-w-2xl mx-auto px-4 sm:px-6 py-8">`
    - Heading: `<h1 class="text-xl font-semibold mb-4">About ErasureKit</h1>`
    - Body paragraph: "ErasureKit is a free, open-source tool that helps Europeans exercise their GDPR Article 17 right to erasure. It generates and sends legally accurate data deletion requests to known data brokers without exposing your real email address."
    - Additional paragraphs about privacy-first design, open source nature, etc.

    **7. src/app.js** -- Root component wiring everything together:
    - Import `html` from `htm/preact`, `render` from `preact`
    - Import `currentPage` from `./lib/router.js`
    - Import `'./lib/theme.js'` (side-effect import to initialize theme effect)
    - Import all components: Header, Stepper, WelcomeScreen, Footer
    - Import all pages: Identity, Brokers, Send, Track, About
    - For production build mode: import `'./styles.css'` (Vite processes this, CDN mode ignores it)
    - Define `WIZARD_STEPS` set: `new Set(['identity', 'brokers', 'send', 'track'])`
    - Define `App` function component:
      ```javascript
      function App() {
        const page = currentPage.value;
        const showStepper = WIZARD_STEPS.has(page);

        return html`
          <div class="min-h-screen flex flex-col bg-[var(--ek-surface)] text-[var(--ek-text)]">
            <${Header} />
            ${showStepper && html`<${Stepper} />`}
            <main class="flex-1">
              ${page === 'welcome' && html`<${WelcomeScreen} />`}
              ${page === 'identity' && html`<${Identity} />`}
              ${page === 'brokers' && html`<${Brokers} />`}
              ${page === 'send' && html`<${Send} />`}
              ${page === 'track' && html`<${Track} />`}
              ${page === 'about' && html`<${About} />`}
              ${page === 'legal' && html`<${PlaceholderPage} title="Legal Reference" phase="7" />`}
              ${page === 'escalation' && html`<${PlaceholderPage} title="Escalation" phase="8" />`}
            </main>
            <${Footer} />
          </div>
        `;
      }
      ```
    - Define a generic `PlaceholderPage` component inline for legal/escalation (just heading + "Coming in Phase N")
    - Mount: `render(html\`<\${App} />\`, document.getElementById('app'));`
    - IMPORTANT for build mode: The `import './styles.css'` line is needed for Vite to process Tailwind. In CDN dev mode, this import will fail silently since there's no bundler -- the CDN `<script>` tag handles Tailwind instead. To avoid the error in dev mode, wrap it in a try-catch or use a conditional: only import styles.css if not in a no-build environment. The cleanest approach: Vite will handle the import; in no-build mode, the browser import map won't resolve it and the CDN handles styling independently. Since the CDN mode uses `<script src>` for Tailwind (not a CSS import), the unresolved import won't cause issues in modern browsers (it will be a module import error caught silently).
    - ALTERNATIVE (simpler): Just include `import './styles.css'` at the top. In dev mode opened directly (file://), this will throw a module resolution error in console but won't break the app since the CDN Tailwind script already provides all utility classes. The app renders fine either way. In Vite build mode, it resolves correctly.

    **8. Post-implementation: Verify production build:**
    After all components are created, run `pnpm build` and verify:
    - `dist/Erasure-Kit/erasure-kit.html` exists (not index.html)
    - `dist/Erasure-Kit/brokers.json` exists (separate file, not inlined)
    - `dist/Erasure-Kit/README.md` exists
    - `erasure-kit.html` is a single file containing all JS and CSS inlined
    - File size is reasonable (under 200KB)
  </action>
  <verify>
    <automated>cd "F:/_Github/Virgo/tools/erasure-kit" && test -f src/app.js && test -f src/components/WelcomeScreen.js && test -f src/pages/Identity.js && test -f src/pages/Brokers.js && test -f src/pages/Send.js && test -f src/pages/Track.js && test -f src/pages/About.js && grep -l "Take back your data" src/components/WelcomeScreen.js && grep -l "currentPage" src/app.js && pnpm build 2>&1 && test -f dist/Erasure-Kit/erasure-kit.html && test -f dist/Erasure-Kit/brokers.json && test -f dist/Erasure-Kit/README.md && echo "BUILD VERIFIED: erasure-kit.html + brokers.json + README.md in dist/Erasure-Kit/"</automated>
  </verify>
  <acceptance_criteria>
    - src/components/WelcomeScreen.js contains "Take back your data" heading text
    - src/components/WelcomeScreen.js contains "Get Started" CTA text
    - src/components/WelcomeScreen.js contains "Resume Campaign" text for returning users
    - src/components/WelcomeScreen.js checks `localStorage.getItem('ek-campaign')` for returning user detection
    - src/components/WelcomeScreen.js contains "No data leaves your browser. No accounts. No tracking." privacy statement
    - src/components/WelcomeScreen.js CTA calls `navigateTo('identity')` on click
    - src/components/WelcomeScreen.js has 4 bullet points with SVG icons
    - src/pages/Identity.js contains "Identity" heading and "Coming in Phase 2"
    - src/pages/Brokers.js contains "Brokers" heading and "Coming in Phase 3"
    - src/pages/Send.js contains "Send" heading and "Coming in Phase 5"
    - src/pages/Track.js contains "Track" heading and "Coming in Phase 5"
    - src/pages/About.js contains "About ErasureKit" heading
    - src/pages/About.js contains "GDPR Article 17" in body text
    - src/app.js imports `currentPage` from `./lib/router.js`
    - src/app.js imports theme.js (side-effect for theme initialization)
    - src/app.js renders Header, conditionally renders Stepper (only for wizard steps), renders page based on currentPage.value, renders Footer
    - src/app.js calls `render()` from preact to mount App to `#app`
    - `pnpm build` completes without errors
    - dist/Erasure-Kit/erasure-kit.html exists (NOT index.html)
    - dist/Erasure-Kit/brokers.json exists as a separate file (not inlined into HTML)
    - dist/Erasure-Kit/README.md exists
    - erasure-kit.html file size is under 200KB
  </acceptance_criteria>
  <done>Complete interactive app shell: welcome screen with hero content and CTA, 4 placeholder step pages, About page, signal-based routing via App component, and verified production build producing dist/Erasure-Kit/ with erasure-kit.html + brokers.json + README.md</done>
</task>

</tasks>

<verification>
1. App shell is navigable: welcome screen visible on load, stepper steps clickable, hamburger menu opens/closes, theme toggles
2. All copy matches the UI-SPEC Copywriting Contract exactly
3. Responsive: stepper labels hidden on mobile, CTA full-width on mobile
4. Accessibility: aria-label on toggle, aria-expanded on menu, aria-current on active step, nav has aria-label
5. Production build: `pnpm build` produces `dist/Erasure-Kit/` with `erasure-kit.html` (single file, all JS/CSS inlined) + `brokers.json` (separate) + `README.md`
6. Dev mode: `src/index.html` loads in browser via local server, all navigation works
</verification>

<success_criteria>
- Opening the app shows the welcome screen with "Take back your data" heading and "Get Started" button
- Clicking "Get Started" navigates to Identity step with stepper visible
- All 4 stepper steps are clickable and navigate between placeholder pages
- Theme toggle switches between dark and light mode, persists across refresh
- Hamburger menu slides in from right with Legal Reference, Escalation, About items
- Footer has clickable Legal Reference and About links
- `pnpm build` produces dist/Erasure-Kit/erasure-kit.html + brokers.json + README.md
- erasure-kit.html works when opened in a browser (all JS/CSS inlined)
</success_criteria>

<output>
After completion, create `.planning/phases/01-app-shell-and-distribution/01-02-SUMMARY.md`
</output>
