---
phase: 02-identity-input-and-persistence
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/components/Header.js
  - src/components/Footer.js
  - src/components/WelcomeScreen.js
  - src/app.js
autonomous: true
requirements:
  - PERS-01
  - PERS-02
  - PERS-03
  - PERS-04

must_haves:
  truths:
    - "User sees Save and Load icon buttons in the header bar on every page"
    - "Clicking Save opens a file picker (or triggers download in Firefox/Safari)"
    - "Clicking Load opens a file picker and restores campaign state"
    - "Footer shows subtle 'Auto-saved' text with checkmark that fades after 2 seconds"
    - "Returning user with localStorage data sees 'Resume Campaign' + 'Start New' buttons"
    - "Returning user sees summary text 'You have an unsaved campaign with N emails to erase'"
    - "'Load from file' text link below the buttons opens file picker"
    - "Start New clears localStorage and starts fresh campaign"
  artifacts:
    - path: "src/components/Header.js"
      provides: "Header with Save/Load icon buttons"
      contains: "saveCampaignToFile"
    - path: "src/components/Footer.js"
      provides: "Footer with auto-save indicator"
      contains: "lastAutoSaved"
    - path: "src/components/WelcomeScreen.js"
      provides: "Welcome screen with Resume/Start New/Load flow"
      contains: "hasExistingCampaign"
    - path: "src/app.js"
      provides: "Root app with campaign.js side-effect import"
      contains: "import './lib/campaign.js'"
  key_links:
    - from: "src/components/Header.js"
      to: "src/lib/campaign.js"
      via: "imports saveCampaignToFile and loadCampaignFromFile"
      pattern: "import.*saveCampaignToFile.*loadCampaignFromFile.*from.*campaign"
    - from: "src/components/Footer.js"
      to: "src/lib/campaign.js"
      via: "imports lastAutoSaved signal for indicator"
      pattern: "import.*lastAutoSaved.*from.*campaign"
    - from: "src/components/WelcomeScreen.js"
      to: "src/lib/campaign.js"
      via: "imports hasExistingCampaign, campaign, startNewCampaign, loadCampaignFromFile"
      pattern: "import.*hasExistingCampaign.*from.*campaign"
    - from: "src/app.js"
      to: "src/lib/campaign.js"
      via: "side-effect import initializes auto-save effect"
      pattern: "import.*campaign"
---

<objective>
Wire campaign persistence into the app shell: Save/Load buttons in Header, auto-save indicator in Footer, and Resume/Start New flow on the Welcome Screen.

Purpose: This plan connects the campaign state module (Plan 01) to the existing app shell components. Users get visible save/load controls, auto-save feedback, and a returning user experience that lets them resume or start fresh.

Output: Modified `Header.js` (Save/Load buttons), `Footer.js` (auto-save indicator), `WelcomeScreen.js` (Resume/Start New/Load), `app.js` (campaign import).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-identity-input-and-persistence/02-CONTEXT.md
@.planning/phases/02-identity-input-and-persistence/02-RESEARCH.md
@.planning/phases/02-identity-input-and-persistence/02-01-SUMMARY.md

<interfaces>
<!-- Campaign module API (created in Plan 01) -->
From src/lib/campaign.js:
```javascript
export const campaign = signal({ version, createdAt, updatedAt, identity: { fullName, emails, phone, address }, brokers, tempEmail, messages, statuses, settings });
export const hasExistingCampaign = signal(boolean);
export const lastAutoSaved = signal(null); // timestamp or null
export function saveCampaignToFile() { /* returns Promise, shows file picker */ }
export function loadCampaignFromFile() { /* returns Promise<{ success?, error?, cancelled? }> */ }
export function startNewCampaign() { /* clears localStorage, resets signal */ }
export const CURRENT_VERSION = 1;
```

From src/lib/router.js:
```javascript
export const currentPage = signal('welcome');
export function navigateTo(page) { currentPage.value = page; }
```

From src/components/Header.js (current):
```javascript
import { html } from 'htm/preact';
import { ThemeToggle } from './ThemeToggle.js';
import { HamburgerMenu } from './HamburgerMenu.js';
export function Header() {
  return html`
    <header class="sticky top-0 z-30 h-14 flex items-center justify-between px-4 bg-[var(--ek-surface-alt)] border-b border-[var(--ek-border)]">
      <span class="text-xl font-semibold text-[var(--ek-primary)]">ErasureKit</span>
      <div class="flex items-center gap-1">
        <${ThemeToggle} />
        <${HamburgerMenu} />
      </div>
    </header>
  `;
}
```

From src/components/Footer.js (current):
```javascript
import { html } from 'htm/preact';
import { navigateTo } from '../lib/router.js';
export function Footer() {
  return html`
    <footer class="h-12 flex items-center justify-center gap-4 px-4 border-t border-[var(--ek-border)] text-sm text-[var(--ek-text-muted)]">
      <button onClick=${() => navigateTo('legal')} class="...">Legal Reference</button>
      <button onClick=${() => navigateTo('about')} class="...">About</button>
    </footer>
  `;
}
```

From src/components/WelcomeScreen.js (current):
```javascript
import { html } from 'htm/preact';
import { navigateTo } from '../lib/router.js';
export function WelcomeScreen() {
  const hasExistingCampaign = typeof localStorage !== 'undefined' && localStorage.getItem('ek-campaign') !== null;
  // ... hero, steps, CTA button, privacy statement
}
```

From src/app.js (current):
```javascript
import './lib/theme.js'; // side-effect import
// ... imports Header, Stepper, WelcomeScreen, Footer, pages
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Save/Load icon buttons to Header and campaign import to app.js</name>
  <files>src/components/Header.js, src/app.js</files>
  <read_first>
    - src/components/Header.js (current header -- modify, do not rewrite from scratch)
    - src/app.js (current app root -- add campaign.js import)
    - src/lib/campaign.js (API being called: saveCampaignToFile, loadCampaignFromFile)
    - .planning/phases/02-identity-input-and-persistence/02-CONTEXT.md (decisions D-14, D-17, D-18)
  </read_first>
  <action>
**Header.js modifications per D-14:**

Add imports at top:
```javascript
import { saveCampaignToFile, loadCampaignFromFile } from '../lib/campaign.js';
import { signal } from '@preact/signals';
```

Add local state for load error feedback:
```javascript
const loadError = signal(null);
```

Add an async click handler for load:
```javascript
async function handleLoad() {
  loadError.value = null;
  const result = await loadCampaignFromFile();
  if (result && result.error === 'invalid') {
    loadError.value = "This file doesn't appear to be a valid ErasureKit campaign";
  } else if (result && result.error === 'parse') {
    loadError.value = "Could not read this file. Try another.";
  }
  // cancelled and success: no action needed
  if (loadError.value) {
    setTimeout(() => { loadError.value = null; }, 5000);
  }
}
```

In the header template, add Save and Load buttons BEFORE the ThemeToggle, inside the `<div class="flex items-center gap-1">`:

Save button (per D-14, D-17 -- always shows file picker):
```html
<button
  onClick=${saveCampaignToFile}
  class="w-10 h-10 rounded-lg flex items-center justify-center text-[var(--ek-text-muted)] hover:text-[var(--ek-text)] hover:bg-[var(--ek-surface)]/50 transition-colors"
  aria-label="Save campaign to file"
  title="Save to file"
>
  <!-- Save/download SVG icon: 20x20, stroke, arrow-down-to-tray -->
  <svg class="w-5 h-5" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
    <path stroke-linecap="round" stroke-linejoin="round" d="M8 7H5a2 2 0 00-2 2v9a2 2 0 002 2h14a2 2 0 002-2V9a2 2 0 00-2-2h-3m-1 4l-3 3m0 0l-3-3m3 3V4" />
  </svg>
</button>
```

Load button (per D-14):
```html
<button
  onClick=${handleLoad}
  class="w-10 h-10 rounded-lg flex items-center justify-center text-[var(--ek-text-muted)] hover:text-[var(--ek-text)] hover:bg-[var(--ek-surface)]/50 transition-colors"
  aria-label="Load campaign from file"
  title="Load from file"
>
  <!-- Upload/folder-open SVG icon: 20x20, stroke -->
  <svg class="w-5 h-5" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
    <path stroke-linecap="round" stroke-linejoin="round" d="M4 16v1a3 3 0 003 3h10a3 3 0 003-3v-1m-4-8l-4-4m0 0L8 8m4-4v12" />
  </svg>
</button>
```

Add error toast below header (conditionally rendered when `loadError.value` is truthy):
```html
${loadError.value && html`
  <div class="fixed top-16 left-1/2 -translate-x-1/2 z-50 bg-[var(--ek-danger)] text-white text-sm px-4 py-2 rounded-lg shadow-lg flex items-center gap-2">
    <span>${loadError.value}</span>
    <button onClick=${() => { loadError.value = null; }} class="text-white/80 hover:text-white">
      <svg class="w-4 h-4" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12"/></svg>
    </button>
  </div>
`}
```
The error toast renders OUTSIDE the `<header>` element but INSIDE the returned fragment. Wrap the return in a fragment: `html\`<div>..header..${loadError toast}</div>\`` or use Preact Fragment.

**app.js modification:**

Add a side-effect import for campaign.js to ensure the auto-save effect is initialized on app load, following the same pattern as theme.js:
```javascript
// Side-effect import: initializes campaign auto-save effect
import './lib/campaign.js';
```
Place this immediately after the `import './lib/theme.js';` line. Do NOT change any other imports or logic in app.js.
  </action>
  <verify>
    <automated>cd "F:/_Github/Virgo/tools/erasure-kit" && grep -c "saveCampaignToFile\|loadCampaignFromFile" src/components/Header.js && grep "import.*campaign" src/app.js</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/Header.js` imports `saveCampaignToFile` and `loadCampaignFromFile` from `../lib/campaign.js`
    - `src/components/Header.js` contains `aria-label="Save campaign to file"` (Save button)
    - `src/components/Header.js` contains `aria-label="Load campaign from file"` (Load button)
    - `src/components/Header.js` contains the string `doesn't appear to be a valid ErasureKit campaign` (D-18 error message)
    - `src/components/Header.js` renders Save button before ThemeToggle (left-to-right order: Save, Load, ThemeToggle, Hamburger)
    - `src/app.js` contains `import './lib/campaign.js'` as a side-effect import
    - `src/app.js` still contains `import './lib/theme.js'` (not removed)
    - `pnpm test` still passes
  </acceptance_criteria>
  <done>Header has Save/Load buttons that call campaign module functions. Load errors show a dismissible toast per D-18. app.js imports campaign.js to initialize auto-save.</done>
</task>

<task type="auto">
  <name>Task 2: Add auto-save indicator to Footer and Resume/Start New flow to WelcomeScreen</name>
  <files>src/components/Footer.js, src/components/WelcomeScreen.js</files>
  <read_first>
    - src/components/Footer.js (current footer -- modify, keep existing nav links)
    - src/components/WelcomeScreen.js (current welcome screen -- replace CTA section with conditional rendering)
    - src/lib/campaign.js (API: lastAutoSaved, hasExistingCampaign, campaign, startNewCampaign, loadCampaignFromFile)
    - .planning/phases/02-identity-input-and-persistence/02-CONTEXT.md (decisions D-15, D-16, D-19, D-20, D-21)
  </read_first>
  <action>
**Footer.js modifications per D-16:**

Add imports:
```javascript
import { lastAutoSaved } from '../lib/campaign.js';
import { signal, effect } from '@preact/signals';
```

Add local state for fade animation:
```javascript
const showAutoSaved = signal(false);
let fadeTimeout = null;

effect(() => {
  if (lastAutoSaved.value) {
    showAutoSaved.value = true;
    if (fadeTimeout) clearTimeout(fadeTimeout);
    fadeTimeout = setTimeout(() => { showAutoSaved.value = false; }, 2000);
  }
});
```

Modify the footer template to add an auto-save indicator. The footer layout becomes a flex row with three sections:
- Left: auto-save indicator (or empty space)
- Center: existing nav links
- Right: empty space (balances layout)

Updated structure:
```html
<footer class="h-12 flex items-center justify-between px-4 border-t border-[var(--ek-border)] text-sm text-[var(--ek-text-muted)]">
  <div class="w-32 flex items-center gap-1 transition-opacity duration-300 ${showAutoSaved.value ? 'opacity-100' : 'opacity-0'}">
    <svg class="w-3.5 h-3.5 text-[var(--ek-success)]" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <path stroke-linecap="round" stroke-linejoin="round" d="M5 13l4 4L19 7"/>
    </svg>
    <span class="text-xs">Auto-saved</span>
  </div>
  <div class="flex items-center gap-4">
    <button onClick=${() => navigateTo('legal')} class="hover:text-[var(--ek-primary)] transition-colors focus-visible:ring-2 focus-visible:ring-sky-500 rounded outline-none">Legal Reference</button>
    <button onClick=${() => navigateTo('about')} class="hover:text-[var(--ek-primary)] transition-colors focus-visible:ring-2 focus-visible:ring-sky-500 rounded outline-none">About</button>
  </div>
  <div class="w-32"></div>
</footer>
```

The `w-32` divs create equal spacing on left/right so the center links remain centered. The auto-save indicator fades in/out with `opacity-100`/`opacity-0` and `transition-opacity duration-300`.

**WelcomeScreen.js modifications per D-19, D-20, D-21:**

Replace the manual localStorage check with campaign module imports:
```javascript
import { hasExistingCampaign, campaign, startNewCampaign, loadCampaignFromFile } from '../lib/campaign.js';
```

Remove the existing line:
```javascript
const hasExistingCampaign = typeof localStorage !== 'undefined' && localStorage.getItem('ek-campaign') !== null;
```

Add a local signal for load error state:
```javascript
const welcomeLoadError = signal(null);
```

Add load handler:
```javascript
async function handleWelcomeLoad() {
  welcomeLoadError.value = null;
  const result = await loadCampaignFromFile();
  if (result && result.success) {
    navigateTo('identity');
  } else if (result && result.error) {
    welcomeLoadError.value = result.error === 'invalid'
      ? "This file doesn't appear to be a valid ErasureKit campaign"
      : "Could not read this file";
    setTimeout(() => { welcomeLoadError.value = null; }, 5000);
  }
}
```

Replace the CTA button section with conditional rendering based on `hasExistingCampaign.value`:

**When `hasExistingCampaign.value` is TRUE (returning user per D-19, D-20, D-21):**
```html
<!-- Summary text per D-20 -->
<p class="text-sm text-[var(--ek-text-muted)] text-center mb-4">
  You have an unsaved campaign with ${campaign.value.identity.emails.filter(e => e.trim() !== '').length || 0} email${campaign.value.identity.emails.filter(e => e.trim() !== '').length !== 1 ? 's' : ''} to erase
</p>

<!-- Dual buttons per D-19 -->
<div class="flex flex-col sm:flex-row justify-center gap-3 mb-4">
  <button
    onClick=${() => navigateTo('identity')}
    class="h-12 px-8 bg-[var(--ek-primary)] text-white text-base font-semibold rounded-lg hover:brightness-90 active:brightness-85 transition-all duration-150 hover:scale-[1.02] focus-visible:ring-2 focus-visible:ring-sky-500 focus-visible:ring-offset-2 focus-visible:ring-offset-[var(--ek-surface)] outline-none"
  >
    Resume Campaign
  </button>
  <button
    onClick=${() => { startNewCampaign(); navigateTo('identity'); }}
    class="h-12 px-8 bg-transparent border border-[var(--ek-border)] text-[var(--ek-text)] text-base font-semibold rounded-lg hover:bg-[var(--ek-surface-alt)] transition-all duration-150 focus-visible:ring-2 focus-visible:ring-sky-500 focus-visible:ring-offset-2 focus-visible:ring-offset-[var(--ek-surface)] outline-none"
  >
    Start New
  </button>
</div>

<!-- Load from file per D-21 -->
<div class="text-center">
  <button
    onClick=${handleWelcomeLoad}
    class="text-sm text-[var(--ek-text-muted)] hover:text-[var(--ek-primary)] underline transition-colors"
  >
    Load from file
  </button>
</div>

<!-- Error message for failed load -->
${welcomeLoadError.value && html`
  <p class="text-sm text-[var(--ek-danger)] text-center mt-2">${welcomeLoadError.value}</p>
`}
```

**When `hasExistingCampaign.value` is FALSE (new user):**
Keep the existing "Get Started" button and structure. The button calls `navigateTo('identity')`.

Keep the hero heading, body paragraph, 4 step bullets, and privacy statement unchanged -- only the CTA button section changes.
  </action>
  <verify>
    <automated>cd "F:/_Github/Virgo/tools/erasure-kit" && grep -c "lastAutoSaved" src/components/Footer.js && grep -c "Auto-saved" src/components/Footer.js && grep -c "Resume Campaign" src/components/WelcomeScreen.js && grep -c "Start New" src/components/WelcomeScreen.js && grep -c "Load from file" src/components/WelcomeScreen.js</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/Footer.js` imports `lastAutoSaved` from `../lib/campaign.js`
    - `src/components/Footer.js` contains the string `Auto-saved` (D-16 indicator text)
    - `src/components/Footer.js` contains `opacity-100` and `opacity-0` (fade transition)
    - `src/components/Footer.js` contains the checkmark SVG path `M5 13l4 4L19 7`
    - `src/components/Footer.js` still contains `Legal Reference` and `About` buttons (preserved)
    - `src/components/WelcomeScreen.js` imports `hasExistingCampaign` from `../lib/campaign.js`
    - `src/components/WelcomeScreen.js` imports `startNewCampaign` from `../lib/campaign.js`
    - `src/components/WelcomeScreen.js` imports `loadCampaignFromFile` from `../lib/campaign.js`
    - `src/components/WelcomeScreen.js` contains `Resume Campaign` (D-19)
    - `src/components/WelcomeScreen.js` contains `Start New` (D-19)
    - `src/components/WelcomeScreen.js` contains `Load from file` (D-21)
    - `src/components/WelcomeScreen.js` contains `emails to erase` (D-20 summary text)
    - `src/components/WelcomeScreen.js` does NOT contain `localStorage.getItem('ek-campaign')` (replaced with campaign module import)
    - `pnpm test` still passes
  </acceptance_criteria>
  <done>Footer shows auto-save indicator that fades after 2 seconds. Welcome screen detects returning users and shows Resume/Start New/Load from file flow. All persistence UI wiring complete.</done>
</task>

</tasks>

<verification>
1. `pnpm test` passes (no regression)
2. `grep "saveCampaignToFile" src/components/Header.js` confirms save button wiring
3. `grep "lastAutoSaved" src/components/Footer.js` confirms auto-save indicator wiring
4. `grep "Resume Campaign" src/components/WelcomeScreen.js` confirms returning user flow
5. `grep "Start New" src/components/WelcomeScreen.js` confirms new campaign flow
6. `grep "Load from file" src/components/WelcomeScreen.js` confirms file load link
7. `grep "import.*campaign" src/app.js` confirms side-effect import
</verification>

<success_criteria>
- Header shows Save/Load icon buttons next to ThemeToggle and Hamburger
- Save button opens file picker (or download in Firefox/Safari) via browser-fs-access
- Load button opens file picker, restores campaign, shows error toast on invalid files
- Footer shows "Auto-saved" checkmark that fades after 2 seconds on each auto-save
- Returning user sees "Resume Campaign" + "Start New" + "Load from file" on welcome screen
- New user sees "Get Started" button (existing behavior preserved)
- app.js imports campaign.js to initialize auto-save on app load
</success_criteria>

<output>
After completion, create `.planning/phases/02-identity-input-and-persistence/02-03-SUMMARY.md`
</output>
