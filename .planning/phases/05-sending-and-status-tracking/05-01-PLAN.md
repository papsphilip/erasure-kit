---
phase: "05"
plan: "01"
type: execution
autonomous: true
wave: 1
depends_on: ["04-02"]
requirements: [SEND-01, SEND-02, SEND-03, SEND-04, STAT-01, STAT-02, STAT-03, STAT-04, STAT-05]
---

# Plan 05-01: Sending Infrastructure, Status Tracking, and Campaign Dashboard

## Objective

Build the complete Phase 5 sending and status tracking system: refactor stepper from 4 to 3 steps (merge Send into Brokers), create status tracking data model with deadline calculation, refactor sidebar to modal, add sending mechanism (mailto: with clipboard fallback), add sticky send bar to Brokers page, and build the Track page campaign dashboard.

## Context

- @.planning/phases/05-sending-and-status-tracking/05-CONTEXT.md — All 60 decisions
- @src/lib/campaign.js — Campaign signal with statuses: {} slot
- @src/components/Stepper.js — Currently 4 steps, needs to become 3
- @src/components/TemplateSidebar.js — Sidebar to be refactored into modal
- @src/pages/Brokers.js — Absorbs send functionality
- @src/pages/Send.js — Placeholder to be deleted
- @src/pages/Track.js — Placeholder to be replaced with dashboard

## Tasks

### Task 1: Status tracking data model and campaign helpers
type="auto"

Create the status tracking infrastructure in the campaign module.

**Create:** `src/lib/status-tracker.js`
- Status enum: `not_selected`, `selected`, `sent`, `awaiting`, `confirmed`, `rejected`, `escalated`, `overdue`
- `updateBrokerStatus(brokerId, status, details)` — immutable campaign update per D-41
- `getBrokerStatus(brokerId)` — returns status object or null
- `getStatusCounts()` — aggregate counts for dashboard per D-26
- `getProgressPercent()` — (confirmed + rejected) / total selected per D-25
- `markBrokerSent(brokerId)` — sets status to 'awaiting', records sentAt + calculates deadline via date-fns addMonths per D-37, D-38
- `checkOverdueBrokers()` — compares today vs deadlines, auto-updates to 'overdue' per D-39, D-40
- `getBrokerHistory(brokerId)` — returns history array per D-30
- Campaign data model: `campaign.statuses[brokerId] = { status, sentAt, deadline, history: [{status, at, details}] }` per D-41

**Install** `date-fns` dependency for deadline calculation.

**Done when:** Status helper functions work correctly with immutable campaign updates.

### Task 2: Stepper refactoring and route cleanup
type="auto"

Refactor the app from 4 wizard steps to 3 per D-07.

**Modify:** `src/components/Stepper.js`
- Change STEPS from 4 items to 3: Identity (1), Brokers (2), Track (3)
- Remove 'send' step entirely

**Modify:** `src/app.js`
- Remove `Send` import and route
- Remove 'send' from WIZARD_STEPS set
- Delete the `${page === 'send' && ...}` line

**Delete:** `src/pages/Send.js` — no longer needed

**Modify:** `src/pages/Brokers.js`
- Update `handleContinue()` to navigate to 'track' instead of 'send'
- Update button text from "Continue to Send" to "Continue to Track"

**Done when:** App shows 3 stepper steps, Send page is removed, Brokers continues to Track.

### Task 3: Sidebar to Modal refactoring
type="auto"

Replace the TemplateSidebar with a centered modal per D-16 through D-22.

**Create:** `src/components/TemplateModal.js`
- Centered modal (max-w-3xl / 768px, full-screen on mobile) per D-16
- Click broker row = open modal per D-17
- Shows broker metadata (name, email, region, legal framework, privacy portal, temp email status, notes) at top per D-18
- Template content below: To, Subject, editable Body textarea per D-18
- Navigation arrows in header: [<-] [Broker Name (3/169)] [->] [X close] per D-19
- Arrows cycle through brokers in current filtered view per D-19
- Send + Copy + Reset buttons in footer per D-20
- Close via X button, Esc key, or backdrop click per D-21
- No keyboard arrow shortcuts (avoids textarea conflict) per D-22
- Export `openModal(brokerId, brokerList)` and `modalOpen` signal

**Modify:** `src/pages/Brokers.js`
- Replace TemplateSidebar import with TemplateModal
- Click broker row = open modal (currently toggles expand)
- Checkbox click stops propagation per D-17
- Chevron click toggles inline expand per D-17, D-23
- Remove sidebar-related margin offset
- Remove TemplateDrawer (replaced by modal functionality)

**Done when:** Clicking a broker row opens the modal with template preview, navigation, and send/copy/reset actions.

### Task 4: Sticky send bar and per-broker send icons on Brokers page
type="auto"

Add sending UI to the Brokers page per D-08 through D-14.

**Modify:** `src/pages/Brokers.js`
- Add sticky bottom bar: "{N} selected . {M} sent -- [Send All] / [Send Remaining (N)]" per D-08
- Bar updates in real-time via signals per D-08
- Per-broker send icon (envelope) on every broker row per D-09
- After sending, icon changes to green checkmark with "Re-send" hover per D-11
- Sent rows dimmed with reduced opacity per D-11
- Confirmation dialog before Send All: "Send erasure requests to {N} brokers? This will take ~{time}." per D-12
- Send buttons disabled with tooltip if identity not filled per D-13
- Send All auto-navigates to Track page per D-10
- Individual sends stay on Brokers page per D-10

**Create:** `src/lib/email-sender.js`
- `sendViaMailto(broker, template)` — generates mailto: URI, opens it per D-01
- `sendViaCopy(broker, template)` — clipboard fallback for long templates per D-04
- `MAILTO_LIMIT` constant (~2000 chars) per SEND-04
- `batchSend(brokerIds, onProgress)` — sequential sending with status updates per D-02, D-03
- Progress callback for live UI updates

**Done when:** Users can send via mailto: links, batch send with progress, clipboard fallback for long templates, per-broker send icons update status.

### Task 5: Track page campaign dashboard
type="auto"

Build the full Track page dashboard per D-24 through D-36.

**Replace:** `src/pages/Track.js` with full dashboard
- Progress bar: (confirmed + rejected) / total selected per D-25
- Color-coded stat cards grid: Sent (blue), Awaiting (amber), Confirmed (green), Rejected (red), Overdue (dark red) per D-26
- Full broker status list below stats per D-27
- Filter by status (dropdown/pills) + sort by deadline/name per D-27
- Per-broker deadline display: "Apr 28 (23 days left)" with color coding per D-28
- Overdue brokers: red tint, "Overdue" badge, "Escalate" button (placeholder) per D-29
- Expandable history rows per broker per D-30
- No manual status changes per D-31
- Empty state: "No requests sent yet" with "Go to Brokers" nudge per D-32
- Real-time updates via signals per D-33
- Success banner when all resolved per D-34
- Same max-w-4xl width as Brokers per D-35
- Auto-detect overdue brokers on page load per D-40

**Done when:** Track page shows complete campaign dashboard with progress bar, stats, broker list, deadline tracking, and overdue detection.

## Verification

1. Stepper shows 3 steps (Identity, Brokers, Track) with no Send step
2. Clicking a broker row opens the template modal with navigation arrows
3. Sending via mailto: opens email client with correct subject/body
4. Long templates (>2000 chars) fall back to clipboard copy
5. Batch sending updates per-broker status to 'awaiting' with correct deadline
6. Track page shows progress bar, stat cards, and broker status list
7. Overdue detection correctly flags brokers past their calendar-month deadline
8. Campaign state persists correctly with statuses via auto-save

## Success Criteria

- All 9 SEND/STAT requirements addressed
- Stepper refactored to 3 steps
- Send page deleted
- Modal replaces sidebar
- Track dashboard fully functional
- Status lifecycle working end-to-end
