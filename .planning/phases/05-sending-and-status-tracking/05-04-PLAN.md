---
phase: 05-sending-and-status-tracking
plan: 04
type: execute
wave: 3
depends_on: ["05-01", "05-02", "05-03"]
files_modified:
  - src/pages/Track.js
  - src/pages/Identity.js
autonomous: false
requirements: [STAT-01, STAT-02, STAT-03, STAT-04, STAT-05, SEND-01, SEND-02]

must_haves:
  truths:
    - "Track page shows progress bar = (confirmed + rejected) / total selected"
    - "Track page shows color-coded stat cards (Sent, Awaiting, Confirmed, Rejected, Overdue)"
    - "Track page shows full broker status list with filter and sort"
    - "Each broker shows deadline date with color-coded countdown"
    - "Overdue brokers highlighted with red background and Escalate button"
    - "Expandable history rows per broker"
    - "Empty state shows nudge to go to Brokers"
    - "Success banner when all brokers resolved"
    - "Identity page has EmailJS configuration section"
  artifacts:
    - path: "src/pages/Track.js"
      provides: "Full campaign dashboard with progress, stats, broker list, deadline display"
      contains: "getAggregateStats"
    - path: "src/pages/Identity.js"
      provides: "EmailJS config section with service ID, template ID, public key inputs"
      contains: "emailjs"
  key_links:
    - from: "src/pages/Track.js"
      to: "src/lib/campaign.js"
      via: "getAggregateStats, getProgressPercentage, campaign signal"
      pattern: "import.*getAggregateStats.*campaign"
    - from: "src/pages/Track.js"
      to: "src/lib/deadline.js"
      via: "getDeadlineInfo, isOverdue for per-broker display"
      pattern: "import.*getDeadlineInfo.*deadline"
    - from: "src/pages/Track.js"
      to: "src/lib/notifications.js"
      via: "markAllRead when Track page loads"
      pattern: "import.*markAllRead.*notifications"
---

<objective>
Build the Track page campaign dashboard and add EmailJS configuration to the Identity page.

Purpose: The Track page is the campaign command center -- showing overall progress, per-broker status with deadlines, overdue alerts, and expandable history. The Identity page needs an EmailJS config section so users can set up automated sending before reaching the Brokers page. This completes the Phase 5 user flow.

Output: Fully functional Track dashboard, Identity page with EmailJS setup, human verification checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-sending-and-status-tracking/05-CONTEXT.md
@.planning/phases/05-sending-and-status-tracking/05-RESEARCH.md

@src/pages/Track.js
@src/pages/Identity.js
@src/lib/campaign.js
@src/lib/deadline.js
@src/lib/email-sender.js
@src/lib/notifications.js
@src/lib/router.js

<interfaces>
<!-- From Plan 01 and 02 outputs -->

From src/lib/campaign.js:
```javascript
export const campaign = signal({...})
export function updateBrokerStatus(brokerId, status, details)
export function getAggregateStats() // -> { total, sent, awaiting, confirmed, rejected, overdue, escalated, acknowledged }
export function getProgressPercentage() // -> 0-100
```

From src/lib/deadline.js:
```javascript
export function calculateDeadline(sentAt)
export function isOverdue(deadline) // -> boolean
export function getDeadlineInfo(deadline) // -> { label, color, overdue, daysLeft }
export function formatDeadlineDate(deadline) // -> 'MMM d' string
```

From src/lib/email-sender.js:
```javascript
export const batchState = signal({ active, cancelled, total, sent, failed, current, log })
export const batchProgress = computed(...)
export function cancelBatch()
```

From src/lib/notifications.js:
```javascript
export function markAllRead()
export function addNotification({ type, message, brokerId })
```

From src/lib/brokers-loader.js:
```javascript
export function loadBrokers() // -> Promise<broker[]>
```

Established UI patterns:
- max-w-4xl mx-auto px-4 py-8 container (per D-35)
- rounded-xl bg-[var(--ek-surface-alt)] shadow-lg cards
- Tailwind v4 utility classes
- html tagged templates with HTM
- Inline SVG icons
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Track page campaign dashboard</name>
  <files>src/pages/Track.js</files>
  <read_first>
    - src/pages/Track.js (current placeholder -- will be completely replaced)
    - src/lib/campaign.js (campaign signal, getAggregateStats, getProgressPercentage, updateBrokerStatus)
    - src/lib/deadline.js (getDeadlineInfo, formatDeadlineDate, isOverdue)
    - src/lib/email-sender.js (batchState, batchProgress, cancelBatch)
    - src/lib/notifications.js (markAllRead)
    - src/lib/brokers-loader.js (loadBrokers to look up broker names)
    - src/pages/Brokers.js (filter/sort UI patterns to reuse)
    - .planning/phases/05-sending-and-status-tracking/05-CONTEXT.md (D-24 through D-36, D-37 through D-44)
  </read_first>
  <action>
    Replace `src/pages/Track.js` with a full campaign dashboard.

    **Component structure:**

    ```
    Track (main component)
    ├── BatchProgressOverlay (shown during active batch sending)
    ├── ProgressBar (overall campaign resolution progress)
    ├── StatCardsGrid (5 color-coded stat cards)
    ├── StatusFilterBar (filter pills + sort dropdown)
    ├── BrokerStatusList (filtered/sorted broker entries)
    │   └── BrokerStatusRow (per-broker with expandable history)
    ├── EmptyState (when no sends yet)
    └── SuccessBanner (when all resolved)
    ```

    **On mount:**
    - Call `markAllRead()` from notifications.js (per D-54: clears badge when visiting Track)
    - Load brokers via `loadBrokers()` (for name lookups)
    - Check for overdue brokers: iterate `campaign.value.statuses`, for each with status 'awaiting' or 'acknowledged', call `isOverdue(entry.deadline)` -- if overdue, call `updateBrokerStatus(brokerId, 'overdue', { details: 'Deadline passed' })` (per D-40)

    **BatchProgressOverlay (per D-03, shown when batchState.value.active):**
    - Full-width overlay at top of Track page
    - Progress bar: `batchProgress.value`% filled, blue bg
    - Text: `"Sending... ${batchState.value.sent + batchState.value.failed.length}/${batchState.value.total}"`
    - Live scrolling log: show last 10 entries from `batchState.value.log` with icons:
      - Sent: green check + broker name
      - Failed: red X + broker name + "(retry)" or "(failed)"
      - Current: spinning/pulsing indicator + broker name
    - Cancel button: calls `cancelBatch()` (per D-14)

    **ProgressBar (per D-25):**
    - `getProgressPercentage()` -> width of filled bar
    - Text: `"${percentage}% Complete (${confirmed + rejected} of ${total} resolved)"`
    - Green fill on gray track, `h-4 rounded-full`

    **StatCardsGrid (per D-26):**
    - Call `getAggregateStats()` to get counts
    - Grid: `grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-5 gap-3`
    - Each card: `rounded-xl p-4` with:
      - Sent: `bg-sky-500/15 text-sky-400` border, count + label
      - Awaiting: `bg-amber-500/15 text-amber-400`
      - Confirmed: `bg-emerald-500/15 text-emerald-400`
      - Rejected: `bg-red-500/15 text-red-400`
      - Overdue: `bg-red-800/15 text-red-300`
    - Per D-26: display-only, not clickable

    **StatusFilterBar:**
    - Filter pills: All / Awaiting / Confirmed / Rejected / Overdue
    - Local signal `statusFilter` defaulting to 'all'
    - Sort dropdown: by Name (asc), Deadline (soonest first), Status
    - Local signal `statusSort` defaulting to 'deadline'

    **BrokerStatusList:**
    - Filter `campaign.value.statuses` by `statusFilter`
    - Sort by `statusSort`
    - For each broker with a status entry, render BrokerStatusRow

    **BrokerStatusRow (per D-27, D-28, D-29, D-30):**
    - Look up broker name from loaded brokers array
    - Left: broker name + status badge (colored pill: awaiting=amber, confirmed=green, rejected=red, overdue=dark red, escalated=orange)
    - Right: deadline display per D-28:
      - `formatDeadlineDate(deadline)` + `(${getDeadlineInfo(deadline).label})`
      - Color: green text (>14 days), amber (7-14), red (<7), dark red (overdue)
    - Per D-29: overdue brokers get `bg-red-500/10` row background, red "Overdue" badge, and "Escalate" button (placeholder -- links to escalation page, per D-29: `navigateTo('escalation')`)
    - Per D-30: expandable history. Chevron toggle reveals history list:
      - Each entry: `${status} — ${formatDate(at)}` with optional details text
      - Example: "Selected Mar 15 -> Awaiting Mar 16 10:42 -> Overdue Apr 17"
    - Per D-31: no manual status change buttons (Phase 6 auto-detection)

    **EmptyState (per D-32):**
    - When `campaign.value.brokers.selected.length === 0` OR no statuses exist:
    - "No requests sent yet" message
    - "Go to Brokers ->" button calling `navigateTo('brokers')`

    **SuccessBanner (per D-34):**
    - When `getProgressPercentage() === 100` and all selected brokers have a resolved status:
    - Green success card: "All brokers resolved! Your data erasure campaign is complete."
    - Buttons: "Save Campaign" (calls saveCampaignToFile) and "Delete Temp Email" (placeholder for Phase 6)

    **Page width:** `max-w-4xl mx-auto px-4 py-8` (per D-35)

    All data reads are reactive via Preact signals (per D-33): `campaign.value.statuses` changes trigger re-render.
  </action>
  <verify>
    <automated>cd F:/_Github/Virgo/tools/erasure-kit && npx vitest run --reporter=verbose 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/pages/Track.js is NOT a placeholder (more than 50 lines)
    - src/pages/Track.js contains `import.*getAggregateStats` from campaign
    - src/pages/Track.js contains `import.*getProgressPercentage` from campaign
    - src/pages/Track.js contains `import.*getDeadlineInfo` from deadline
    - src/pages/Track.js contains `import.*isOverdue` from deadline
    - src/pages/Track.js contains `import.*markAllRead` from notifications
    - src/pages/Track.js contains `import.*batchState` from email-sender
    - src/pages/Track.js contains stat card text like "Awaiting" and "Confirmed" and "Overdue"
    - src/pages/Track.js contains "No requests sent yet" (empty state text)
    - src/pages/Track.js contains "All brokers resolved" or similar success banner text
    - src/pages/Track.js contains filter functionality (statusFilter signal or equivalent)
    - All tests pass: `npx vitest run` exits with code 0
  </acceptance_criteria>
  <done>Track page is a complete campaign dashboard with progress bar, stat cards, filterable broker status list with deadline countdowns, overdue highlighting, expandable history, batch progress overlay, empty state, and success banner.</done>
</task>

<task type="auto">
  <name>Task 2: Identity page EmailJS configuration section</name>
  <files>src/pages/Identity.js</files>
  <read_first>
    - src/pages/Identity.js (current identity form -- add EmailJS config section below optional fields)
    - src/lib/campaign.js (campaign signal -- settings field for EmailJS config)
    - .planning/phases/05-sending-and-status-tracking/05-CONTEXT.md (D-13, D-45, D-46)
    - .planning/phases/05-sending-and-status-tracking/05-RESEARCH.md (EmailJS configuration requirements: publicKey, serviceId, templateId)
  </read_first>
  <action>
    **Add EmailJS configuration section to Identity page (per D-13, research open question 1):**

    Add a new collapsible section below the "Optional: Phone & Address" section, titled "Email Sending Setup" with a mail icon.

    The section contains:
    - Brief explanation text: "ErasureKit uses EmailJS to send erasure requests automatically. Create a free account at emailjs.com (200 emails/month), then enter your credentials below."
    - Link to EmailJS: `<a href="https://www.emailjs.com" target="_blank">emailjs.com</a>`
    - Three input fields:
      1. **Public Key** -- label: "Public Key", placeholder: "Your EmailJS public key", helper: "Found in EmailJS Dashboard -> Account -> General"
      2. **Service ID** -- label: "Service ID", placeholder: "e.g. service_gmail", helper: "Found in EmailJS Dashboard -> Email Services"
      3. **Template ID** -- label: "Template ID", placeholder: "e.g. template_gdpr", helper: "Found in EmailJS Dashboard -> Email Templates"
    - Values stored in `campaign.value.settings.emailjs = { publicKey, serviceId, templateId }`
    - Use the same updateIdentity pattern but for settings:
      ```javascript
      function updateEmailJSConfig(field, value) {
        campaign.value = {
          ...campaign.value,
          updatedAt: new Date().toISOString(),
          settings: {
            ...campaign.value.settings,
            emailjs: {
              ...(campaign.value.settings.emailjs || {}),
              [field]: value,
            },
          },
        };
      }
      ```
    - Green checkmark indicator when all three fields are filled (non-empty)
    - If all three empty, show info text: "Without EmailJS setup, you can still use manual mailto: links and clipboard copy to send requests."

    Use a local signal `showEmailSetup` for the collapsible toggle, matching the `showOptional` pattern.

    The inputs use the same `BASE_INPUT_CLASSES` and styling as the existing identity fields.

    **Note on D-45 (temp email creation):** D-45 says users create temp email in Identity step. However, mail.tm integration (POST /accounts) is a Phase 6 requirement (TEMP-01). For Phase 5, the Identity page only has the EmailJS config. Phase 6 will add the "Create temp email" button here.
  </action>
  <verify>
    <automated>cd F:/_Github/Virgo/tools/erasure-kit && npx vitest run --reporter=verbose 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/pages/Identity.js contains "Email Sending Setup" or "EmailJS" section title text
    - src/pages/Identity.js contains input fields for publicKey, serviceId, and templateId
    - src/pages/Identity.js contains `emailjs.com` link
    - src/pages/Identity.js contains a function that updates `campaign.value.settings.emailjs`
    - src/pages/Identity.js contains "Public Key" label text
    - src/pages/Identity.js contains "Service ID" label text
    - src/pages/Identity.js contains "Template ID" label text
    - All tests pass: `npx vitest run` exits with code 0
  </acceptance_criteria>
  <done>Identity page has collapsible EmailJS configuration section with three input fields (publicKey, serviceId, templateId) persisted in campaign.settings.emailjs. Inputs use consistent styling with existing form fields.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of complete Phase 5 flow</name>
  <what-built>
    Complete Phase 5: Sending and Status Tracking implementation including:
    1. Campaign schema v3 with per-broker status tracking and deadline calculation
    2. Email sender library with EmailJS integration and mailto/clipboard fallback
    3. Notification system (toasts + bell icon)
    4. 3-step wizard (Send page removed)
    5. BrokerModal replacing sidebar
    6. Brokers page with send bar, per-broker send icons, batch send
    7. Track page campaign dashboard
    8. Identity page EmailJS configuration
  </what-built>
  <how-to-verify>
    Start the dev server: `cd F:/_Github/Virgo/tools/erasure-kit && pnpm dev`
    Open browser to http://localhost:5173

    **Step 1: Verify 3-step wizard**
    - Stepper should show: Identity (1) -> Brokers (2) -> Track (3)
    - No "Send" step should exist

    **Step 2: Verify Identity page**
    - Enter name and email
    - Expand "Email Sending Setup" section
    - See three input fields for EmailJS config (Public Key, Service ID, Template ID)
    - Continue to Brokers

    **Step 3: Verify Brokers page**
    - Select some brokers
    - Click a broker ROW -> should open centered modal with metadata + template
    - Click chevron icon -> should expand inline details (NOT open modal)
    - Check that per-broker envelope send icons appear on each row
    - Check sticky bottom bar showing "X selected -- Send Remaining"
    - Send buttons should be disabled/show tooltip if no EmailJS config entered

    **Step 4: Verify BrokerModal**
    - Modal has navigation arrows [<-] [Broker Name (N/Total)] [->]
    - Modal shows To, Subject, editable Body
    - Send, Copy, Reset buttons in footer
    - Close via X, Esc, or click outside

    **Step 5: Verify Track page**
    - Navigate to Track step
    - If no sends: should show "No requests sent yet" with "Go to Brokers" nudge
    - Verify stat cards grid (Sent, Awaiting, Confirmed, Rejected, Overdue) all at 0
    - Verify progress bar at 0%

    **Step 6: Verify Header**
    - Bell icon present in header between theme toggle and hamburger menu
    - Click bell -> dropdown appears with "No notifications yet"

    **Step 7: Verify production build**
    - Run: `pnpm build`
    - Open `dist/index.html` in browser
    - Verify same functionality works in production build
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
</task>

</tasks>

<verification>
1. `npx vitest run --reporter=verbose` -- all tests pass
2. `pnpm dev` -- dev server starts, all pages render
3. `pnpm build` -- production build succeeds
4. Manual walkthrough of the 7-step verification above
</verification>

<success_criteria>
- Track page shows complete campaign dashboard with progress, stats, broker list, deadlines
- Identity page has EmailJS configuration section
- All Phase 5 requirements (SEND-01 through SEND-04, STAT-01 through STAT-05) satisfied
- Production build succeeds
- Human verified
</success_criteria>

<output>
After completion, create `.planning/phases/05-sending-and-status-tracking/05-04-SUMMARY.md`
</output>
