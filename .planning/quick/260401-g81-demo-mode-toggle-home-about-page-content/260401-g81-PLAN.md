---
phase: quick
plan: 260401-g81
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/demo-mode.js
  - src/lib/email-sender.js
  - src/components/Header.js
  - src/components/WelcomeScreen.js
  - src/pages/About.js
autonomous: true
requirements: [DEMO-MODE, HOME-CONTENT, ABOUT-CONTENT]

must_haves:
  truths:
    - "Header shows a visible Demo Mode toggle switch"
    - "When Demo Mode is ON, sending to a broker does NOT open mailto: or clipboard -- it simulates the send"
    - "When Demo Mode is ON, broker is still marked as sent and a notification is shown"
    - "Home page shows CTA section at top followed by how-it-works explanation sections"
    - "About page has expanded content covering legal context, privacy tech details, broker DB, community, licence"
  artifacts:
    - path: "src/lib/demo-mode.js"
      provides: "Demo mode signal and state"
      exports: ["demoMode"]
    - path: "src/lib/email-sender.js"
      provides: "Email sending with demo mode interception"
      contains: "demoMode"
    - path: "src/components/Header.js"
      provides: "Header with demo mode toggle switch"
      contains: "demoMode"
    - path: "src/components/WelcomeScreen.js"
      provides: "Home page with CTA + how-it-works sections"
      min_lines: 80
    - path: "src/pages/About.js"
      provides: "Expanded about page"
      min_lines: 80
  key_links:
    - from: "src/components/Header.js"
      to: "src/lib/demo-mode.js"
      via: "import demoMode signal"
      pattern: "import.*demo-mode"
    - from: "src/lib/email-sender.js"
      to: "src/lib/demo-mode.js"
      via: "checks demoMode.value before sending"
      pattern: "demoMode\\.value"
---

<objective>
Add a Demo Mode toggle to the header that intercepts email sending (simulates instead of opening mailto:/clipboard), enrich the Home page with how-it-works content below the CTA, and expand the About page with detailed legal, privacy, and community information.

Purpose: Demo mode allows showcasing the app without triggering real email clients. Richer Home/About pages improve onboarding and trust for new users.
Output: 5 files modified -- new demo-mode module, updated email-sender, header with toggle, enriched WelcomeScreen, expanded About page.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@src/lib/email-sender.js
@src/lib/notifications.js
@src/lib/status-tracker.js
@src/components/Header.js
@src/components/WelcomeScreen.js
@src/pages/About.js
@src/lib/router.js
@src/app.js

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/lib/email-sender.js:
```javascript
export function sendToBroker(broker, options = {})  // returns { method, success }
export function batchSend(brokers, options = {})     // returns Promise<{ sent, failed, clipboard }>
export function buildMailtoUrl(to, subject, body)    // returns string
export const MAILTO_LIMIT = 2000;
```

From src/lib/notifications.js:
```javascript
export function addNotification({ type, message, brokerId, showToast })  // returns id
```

From src/lib/status-tracker.js:
```javascript
export function markBrokerSent(brokerId)  // marks broker as awaiting + sets deadline
```

From src/lib/campaign.js:
```javascript
export const campaign = signal(...)          // campaign state signal
export const hasExistingCampaign = computed(...)
```

Preact stack: htm/preact for templates, @preact/signals for state, inline SVG icons (no icon library).
CSS: Tailwind classes with CSS custom properties (--ek-primary, --ek-surface, --ek-text, --ek-text-muted, --ek-border, --ek-surface-alt, --ek-danger).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create demo-mode module and wire into email-sender + header</name>
  <files>src/lib/demo-mode.js, src/lib/email-sender.js, src/components/Header.js</files>
  <action>
    1. Create `src/lib/demo-mode.js`:
       - Export a Preact signal `demoMode` initialized to `false` (boolean signal)
       - That is the entire module -- just the signal. Keep it minimal.

    2. Modify `src/lib/email-sender.js` -- add demo mode interception to `sendToBroker()`:
       - Import `demoMode` from `./demo-mode.js`
       - Import `addNotification` from `./notifications.js`
       - At the top of `sendToBroker()`, before any mailto:/clipboard logic, check `if (demoMode.value)`:
         - Log to console: `console.log('[Demo Mode] Simulated send to:', broker.name, broker.email)`
         - Call `markBrokerSent(broker.id)` (already called in normal path, reuse)
         - Call `addNotification({ type: 'success', message: \`[Demo] Simulated send to \${broker.name}\`, brokerId: broker.id })`
         - Return `{ method: 'demo', success: true }` -- early return, skip mailto/clipboard
       - The `batchSend()` function calls `sendToBroker()` internally, so it automatically inherits demo behavior. No changes needed there.

    3. Modify `src/components/Header.js` -- add demo mode toggle switch:
       - Import `demoMode` from `../lib/demo-mode.js`
       - Add a toggle switch in the header's right-side button group, BEFORE the save button (leftmost position in the group, so it is visually prominent).
       - Toggle UI: a compact pill-shaped toggle with label "Demo" next to it.
         - Use a `<button>` with `role="switch"` and `aria-checked` bound to `demoMode.value`.
         - Pill shape: `w-9 h-5 rounded-full` background that is `bg-[var(--ek-text-muted)]/30` when off, `bg-emerald-500` when on.
         - Inner circle: `w-3.5 h-3.5 rounded-full bg-white` that translates right (`translate-x-4`) when on, `translate-x-0.5` when off. Use `transition-transform duration-150`.
         - Label text "Demo" in `text-xs text-[var(--ek-text-muted)]` to the left of the toggle pill.
         - onClick: `demoMode.value = !demoMode.value`
       - When demo mode is active, optionally add a subtle emerald glow or the text turns emerald to indicate active state.
       - Wrap the label + toggle in a `flex items-center gap-1.5` container.
  </action>
  <verify>
    <automated>cd "F:/_Github/Virgo/tools/erasure-kit" && node -e "import('./src/lib/demo-mode.js').then(m => { console.log('demoMode signal:', typeof m.demoMode, m.demoMode.value === false ? 'PASS' : 'FAIL'); })" 2>/dev/null || echo "Module check requires Vite dev server -- verify visually"</automated>
  </verify>
  <done>
    - Demo mode toggle visible in header between ErasureKit brand and save/load buttons
    - Clicking toggle changes its visual state (off=gray, on=emerald)
    - When demo mode ON: clicking send on any broker logs to console, marks broker as sent, shows notification toast, does NOT open mailto: or clipboard
    - When demo mode OFF: normal mailto:/clipboard behavior unchanged
  </done>
</task>

<task type="auto">
  <name>Task 2: Enrich WelcomeScreen with how-it-works sections below CTA</name>
  <files>src/components/WelcomeScreen.js</files>
  <action>
    Keep the entire existing CTA section at the top (shield emoji, hero heading, body paragraph, 4 step bullets, CTA buttons, privacy statement) -- do NOT modify any of it. Only ADD content below.

    After the privacy statement (`</p>` at the bottom), add a horizontal divider and the following sections. Use the same max-w-2xl container. Style consistently with the app's existing design tokens.

    Sections to add (concise, CTA-first -- this is the home page, not the about page):

    1. **"Your Right to Be Forgotten"** section:
       - Brief explanation of GDPR Article 17 -- the right to erasure
       - One sentence: data brokers collect and sell personal info, often without consent
       - One sentence: under GDPR, you can demand deletion
       - Keep to 3-4 sentences max

    2. **"How It Works"** section:
       - 3 numbered steps with icons (reuse the existing inline SVG pattern):
         a. "Enter your details" -- you provide your name and email addresses you want erased
         b. "Select brokers" -- choose from 169+ known data brokers across Europe
         c. "Send requests" -- the app generates legally-compliant erasure emails and opens your email client to send them
       - Each step: icon on left, title bold, description text-muted below

    3. **"Privacy by Design"** section:
       - 3 bullet points with lock/shield SVG icons:
         a. "Runs entirely in your browser" -- no server, no cloud, no data leaves your device
         b. "No accounts or tracking" -- no cookies, no analytics, no sign-up
         c. "Open source" -- inspect the code, contribute, fork it

    Styling rules:
    - Each section: `mt-12` spacing between sections
    - Section headings: `text-lg font-semibold mb-3 text-center`
    - Body text: `text-sm text-[var(--ek-text-muted)] leading-relaxed`
    - Use `var(--ek-primary)` for accent icons
    - Divider before first section: `border-t border-[var(--ek-border)] mt-10 pt-10`
    - Keep all content within the existing `max-w-2xl mx-auto` wrapper
  </action>
  <verify>
    <automated>cd "F:/_Github/Virgo/tools/erasure-kit" && node -e "import('./src/components/WelcomeScreen.js').then(m => console.log('WelcomeScreen exports:', Object.keys(m).join(', '))).catch(e => console.log('Requires Vite -- verify visually'))"</automated>
  </verify>
  <done>
    - Home page renders CTA section at top (unchanged from current)
    - Below CTA: "Your Right to Be Forgotten" section with GDPR context
    - Below that: "How It Works" with 3 numbered steps
    - Below that: "Privacy by Design" with 3 bullet points
    - All sections styled consistently with the app's design tokens
    - Mobile responsive (max-w-2xl with px-4 padding)
  </done>
</task>

<task type="auto">
  <name>Task 3: Expand About page with detailed legal, privacy, technical, and community content</name>
  <files>src/pages/About.js</files>
  <action>
    Replace the current About page content with an expanded version. Same general topics as the home page but significantly more detailed. Keep the same component structure (`export function About()`).

    Sections (in order):

    1. **"About ErasureKit"** (intro):
       - What it is: free, open-source GDPR erasure tool
       - Who it is for: Europeans wanting to exercise data rights
       - What it does: automates sending legally-compliant deletion requests to data brokers

    2. **"GDPR Article 17: The Right to Erasure"** (legal context):
       - Explain Article 17 in accessible language
       - When it applies: personal data processed without consent, no longer necessary, withdrawn consent
       - What organisations must do: respond within one calendar month, confirm deletion or provide lawful basis for refusal
       - Note: this is a RIGHT, not a request -- organisations are legally obligated to comply
       - Mention that failure to comply can result in regulatory complaints to data protection authorities

    3. **"How the App Works"** (step-by-step technical):
       - Step 1: Enter identity (name, email addresses associated with broker accounts)
       - Step 2: Browse and select from 169+ known data brokers (categorized by region and type)
       - Step 3: Preview generated erasure emails (legally-compliant, citing correct GDPR articles)
       - Step 4: Send via your email client (mailto: links) or copy to clipboard for manual sending
       - Step 5: Track responses and enforce 30-day compliance deadlines

    4. **"Privacy First"** (technical privacy details):
       - Everything runs in the browser -- no server, no backend, no API calls to ErasureKit servers
       - No tracking, no cookies, no analytics of any kind
       - Your personal data (name, emails) is stored only in your browser's local storage
       - You can save/load campaigns as JSON files on your own device
       - The app itself is a single HTML file you can download and run offline
       - No accounts, no sign-up, no email verification

    5. **"The Broker Database"** (how broker data works):
       - Community-maintained database of 169+ data brokers across Europe
       - Categorized by region (EU-wide, UK, DACH, Nordics) and type (people search, marketing, credit, etc.)
       - The database is a separate JSON file updated independently of the app
       - Community contributions welcome -- submit new brokers or corrections via GitHub

    6. **"Open Source"** (community + licence):
       - ErasureKit is free software
       - Source code available on GitHub
       - Contributions welcome: broker data, translations, code improvements
       - Licence: mention it is open source (do not specify a specific licence string -- just say "open-source licence")

    Styling rules:
    - Container: `max-w-2xl mx-auto px-4 sm:px-6 py-8`
    - Page title: `text-xl font-semibold mb-6`
    - Section headings: `text-lg font-semibold mt-8 mb-3 text-[var(--ek-text)]`
    - Body text: `text-sm text-[var(--ek-text-muted)] leading-relaxed`
    - Bullet lists: `list-disc list-inside space-y-1 text-sm text-[var(--ek-text-muted)]`
    - Use `<strong class="text-[var(--ek-text)]">` for emphasis within paragraphs
    - Paragraphs: `mb-3` spacing
    - Keep clean, readable, no images or icons needed (text-heavy informational page)
  </action>
  <verify>
    <automated>cd "F:/_Github/Virgo/tools/erasure-kit" && node -e "import('./src/pages/About.js').then(m => console.log('About exports:', Object.keys(m).join(', '))).catch(e => console.log('Requires Vite -- verify visually'))"</automated>
  </verify>
  <done>
    - About page renders with 6 distinct content sections
    - GDPR Article 17 section explains legal context in accessible language
    - Privacy section details that everything is client-side with no tracking
    - Broker database section mentions 169+ brokers and community contributions
    - Open source section invites contributions
    - All text is readable, well-spaced, styled with app design tokens
  </done>
</task>

</tasks>

<verification>
1. Run the Vite dev server (`pnpm dev`) and verify:
   - Header shows "Demo" toggle switch
   - Toggle switches between gray (off) and emerald (on) states
   - With demo mode ON, navigating to brokers page and sending a request does NOT open mailto: -- instead shows a notification toast and marks broker as sent
   - Home page shows CTA at top followed by how-it-works sections
   - About page (via hamburger menu) shows expanded content with all 6 sections
2. With demo mode OFF, sending works normally (opens mailto: or copies to clipboard)
</verification>

<success_criteria>
- Demo Mode toggle visible and functional in header
- Email sending intercepted when demo mode is active (no mailto:, no clipboard, broker still marked sent)
- Home page has CTA + 3 informational sections below
- About page has 6 detailed content sections
- All styling consistent with existing ErasureKit design tokens
</success_criteria>

<output>
After completion, create `.planning/quick/260401-g81-demo-mode-toggle-home-about-page-content/260401-g81-SUMMARY.md`
</output>
